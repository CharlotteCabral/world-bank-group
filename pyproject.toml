[project]
name = "world-bank-group"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Charlotte Cabral", email = "cabral.charlotte@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-bigquery>=1.12.0",
]

[project.scripts]
world-bank-group = "world_bank_group:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
